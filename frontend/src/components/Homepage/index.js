import React from 'react';

export default function Homepage()
{
    // const [user, setUser] = useState([]);
    // const [result, setResult] = useState('');
    // console.log(user);
    // console.log(result);

    // useEffect(() => {
    //     // fetch('/try?name=react')
    //     // .then(res => res.json())
    //     // .then(user => setUser(user));
    //     // fetch('/insert-new-card?name=two name&image=https://images.pexels.com/photos/7708952/pexels-photo-7708952.jpeg?auto=compress&cs=tinysrgb&dpr=2&h=650&w=940')
    //     // .then(res => res.json())
    //     // .then(result => setResult(result));
    // }, []);

    return (
        <div>
            {/* <h1>{result}</h1>
            {user.length === 1 ? 
            <div>
                <h1>{user[0].name}</h1>
                <img src={user[0].image} />
            </div> : 'null'} */}
        </div>
    )
}
